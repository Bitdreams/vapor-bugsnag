# Swift / SwiftPM
.build/
.swiftpm/
*.xcodeproj
Package.resolved
DerivedData/

# macOS
.DS_Store

# Env / secrets — never commit BUGSNAG_KEY
.env
.env.*

# Claude Code local overlays — org-internal, never commit (repo is public)
CLAUDE.local.md
.claude/settings.local.json
.claude/worktrees/
claude/
